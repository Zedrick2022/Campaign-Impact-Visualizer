# Zedrick Salupito — ICP/UVP Landing Page

Upload `index.html`, `style.css`, and `script.js` to the ROOT of your GitHub repository.

GitHub Pages settings:
Settings → Pages → Deploy from a branch → Main → /(root)

The form uses FormSubmit and sends the checklist download link automatically after submission.


